| 待补充数据 | 模拟使用的开源数据集 | 对应负责人 | 模拟用途说明 | 备注 |
|:---|:---|:---|:---|:---|
| **康复疗效评估登记数据** (ICF架构) | **自生成仿真数据** (基于ICF临床表字段逻辑) | **周亦沁** | 构建ICF数据质控体系，通过LSTM-Transformer模型预测从“初评”到“末评”的康复轨迹，输出 `icf_time_series.csv` 供融合模块使用。 | 因无合适公开ICF时序数据集，先仿真模拟；待模型验证后迁移至医院真实数据。 |
| **步态骨骼特征数据** (Gait Data) | **CASIA-B 步态数据集** (图像序列) | **王浩然** | 利用SkeletonGait方法从CASIA-B图像中提取骨骼特征，构建LSCTNN模型识别异常步态，输出 `gait_features.csv`（含膝/踝角度、异常概率等），为ICF“移动”条目提供客观评价。 | 数据集已下载，包含多视角、多状态行走数据。数据为图像格式，需提取骨骼点。后续可能结合其他数据集（如HuGaDB）补充力/标记点信息。 |
| **传感器动作信号** (IMU数据) | **UCI-HAR Dataset** | **周子昊** | 基于1D-CNN/LSTM进行动作识别，评价康复训练动作规范性，输出 `imu_action_scores.csv`（含动作、概率、质量评分）。 | 数据集链接：[UCI-HAR](https://archive.ics.uci.edu/dataset/240/human+activity+recognition+using+smartphones)，用于基准测试。 |
| **多模态融合特征** (综合风险评分) | **前三个模块输出的CSV文件** (`icf_time_series.csv`, `gait_features.csv`, `imu_action_scores.csv`) | **胡昕璟** | 融合多模态特征，计算综合风险评分 `risk_score`，为康复评估提供整体量化指标。 | 不直接使用开源数据集；依赖于其他模块的输出结果，已收集多模态学习资料供参考。 |